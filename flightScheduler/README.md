

Contains the modules for the flight scheduler (booking system). Navigate to the flightScheduler-tests to run tests and flightScheduler to run benchmark experiments.

```
curl -X POST -H "Content-Type: application/json" -d '{"customerId":"1", "flightId": "1", "seatNumber": "1A" }' http://localhost:8766 
```